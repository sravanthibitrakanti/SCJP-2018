package chapter1;

public class LegalIdentifiers {
	//Legal identifiers must start with a letter,currency character($),underscroe (_)
	int a = 10;
	int b$ = 20;
	int c_ = 30;
	//int d@ =40 ; --This gives compile time error 
	//int d@ is illegal because it has special character.Remember only $ and _ are allowed

}
